# Maintainer: Bernhard Landauer <oberon@manjaro.org>

pkgname=xfce-sx-settings
pkgver=20170806
pkgrel=4
pkgdesc="Manjaro SX Xfce settings"
arch=('any')
_repo=project-sx
_commit=60c2e8c49d6b1f055640938c68614703bb11c775
url="https://github.com/philmmanjaro/$_repo"
license=('GPL3')
depends=('manjaro-sx-wallpapers'
	'manjaro-base-skel'
	'manjaro-sx-icons'
	'qt5ct'
	'qt5-styleplugins'
	'vibrancy-colors'
	'adapta-maia-theme'
	'xcursor-breeze')
makedepends=('git')
conflicts=('manjaro-desktop-settings')
provides=('manjaro-desktop-settings')
source=("git+$url.git")
md5sums=('SKIP')

pkgver() {
	date +%Y%m%d
}

package() {
	cd $_repo/settings/$pkgname
	install -d $pkgdir/etc
	cp -r skel $pkgdir/etc
	install -d $pkgdir/usr/share/glib-2.0/schemas
	cp schemas/* $pkgdir/usr/share/glib-2.0/schemas
}
